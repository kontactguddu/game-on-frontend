import { TitleTransformPipe } from './title-transform.pipe';

describe('TitleTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
